userDashboards:
  handler: dashboards/dashboardsFunction/js/userDashboards.userDashboards
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-dashboards/users
        method: POST
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true   
