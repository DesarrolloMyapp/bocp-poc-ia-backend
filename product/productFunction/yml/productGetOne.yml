productGetOne:
  handler: product/productFunction/js/productGetOne.productGetOne
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-product/one/{one}
        method: GET
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true
