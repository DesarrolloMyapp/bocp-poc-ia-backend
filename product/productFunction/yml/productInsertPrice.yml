productInsertPrice:
  handler: product/productFunction/js/productInsertPrice.productInsertPrice
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-product/price/save
        method: POST
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true   
