roleGetOneElement:
  handler: role/roleFunction/js/roleGetOneElement.roleGetOneElement
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-roleId/id/{id}/one/{one}
        method: GET
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true
