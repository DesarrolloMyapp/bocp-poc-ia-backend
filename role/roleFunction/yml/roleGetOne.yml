roleGetOne:
  handler: role/roleFunction/js/roleGetOne.roleGetOne
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-roleOne/one/{one}
        method: GET
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true
