userGetOne:
  handler: user/userFunction/js/userGetOne.userGetOne
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-user/one/{one}
        method: GET
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true 
