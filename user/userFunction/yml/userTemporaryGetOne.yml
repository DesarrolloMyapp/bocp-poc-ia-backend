userTemporaryGetOne:
  handler: user/userFunction/js/userTemporaryGetOne.userTemporaryGetOne
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-user/temporary/one/{one}
        method: GET
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true 
