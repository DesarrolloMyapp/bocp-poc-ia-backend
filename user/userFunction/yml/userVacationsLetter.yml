userVacationsLetter:
  handler: user/userFunction/js/userVacationsLetter.userVacationsLetter
  role: executeLambda
  environment:
    stage: ${self:provider.stage}
    region: ${self:provider.region}
  events:
    - http:
        path: ${self:app}-user/userVacationsLetter/
        method: POST
        integration: lambda
        cors: true 
