userInsert:
  handler: user/userFunction/js/userInsert.userInsert
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-user/
        method: POST
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true   
