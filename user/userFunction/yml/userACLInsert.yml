userACLInsert:
  handler: user/userFunction/js/userACLInsert.userACLInsert
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-user/userACLInsert
        method: POST
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true   
