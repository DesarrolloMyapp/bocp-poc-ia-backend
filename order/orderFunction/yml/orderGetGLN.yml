orderGetGLN:
  handler: order/orderFunction/js/orderGetGLN.orderGetGLN
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-order/registry
        method: POST
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true   
