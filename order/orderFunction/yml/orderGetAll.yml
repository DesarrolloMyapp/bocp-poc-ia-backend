orderGetAll:
  handler: order/orderFunction/js/orderGetAll.orderGetAll
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-order
        method: GET
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true
