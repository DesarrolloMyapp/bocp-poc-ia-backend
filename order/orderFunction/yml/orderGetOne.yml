orderGetOne:
  handler: order/orderFunction/js/orderGetOne.orderGetOne
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-order/one/{one}
        method: GET
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true
