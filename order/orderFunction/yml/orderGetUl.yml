orderGetUl:
  handler: order/orderFunction/js/orderGetUl.orderGetUl
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-order/one/ul/{one}
        method: GET
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true   
