orderResponseGetOne:
  handler: order/orderFunction/js/orderResponseGetOne.orderResponseGetOne
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-order/response/one/{one}
        method: GET
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true
