orderGetAllByCompany:
  handler: order/orderFunction/js/orderGetAllByCompany.orderGetAllByCompany
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-order/corporation/{corporation}
        method: GET
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true
