orderInsert:
  handler: order/orderFunction/js/orderInsert.orderInsert
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-order/save
        method: POST
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true   
