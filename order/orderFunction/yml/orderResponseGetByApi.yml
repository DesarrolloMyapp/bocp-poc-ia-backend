orderResponseGetByApi:
  handler: order/orderFunction/js/orderResponseGetByApi.orderResponseGetByApi
  environment:
    stage: ${self:provider.stage}
  events:
    - http:
        path: ${self:app}-order-response/getMyOrders
        method: POST
        integration: lambda
        authorizer:
          type: COGNITO_USER_POOLS
          authorizerId:
            Ref: apiGatewayAuthorizer
        cors: true   
